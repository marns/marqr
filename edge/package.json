{
    "name": "marqr-edge",
    "private": true,
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "wrangler dev",
        "build": "pnpm --dir ../web run build && wrangler build",
        "deploy": "pnpm run build && wrangler deploy"
    },
    "devDependencies": {
        "@cloudflare/workers-types": "^4.20251121.0",
        "wrangler": "^4.50.0"
    }
}